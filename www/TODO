- Add settings to unlock features like bit order, trailing bits, ignore,
  translate, and wrap
- Add games/tutorials (backed up in local storage) unlocking settings
  - Enter 3 different inputs that decode to "hello" with GD3BWuF6sDkrLw
- Add support for file upload/download
- Advertise the binary (cargo install) and the library (code snippet to create
  the encoding)
